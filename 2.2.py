"""
**"sıra sende sende farklı genişlikte ve renkte bir kare çiz”**

Kaplumbağanın çizgi kalınlığını değiştir, hızını ayarla ve farklı noktalara ışınla.
"""

import turtle 

t = turtle.Turtle()
t.shape("turtle")

t.speed()    # hız (sadece speed kullanıldı)
t.width()    # kenar kalınlığı 5
t.color("renk belirtin")
# kaplumbağayı rastgele noktalara ışınlayın ve ışınladığınız noktada dot ve cirle komutlarını kullanın
t.goto(,)
t.dot(,)
t.goto(, )
t.circle()
t.goto(,)
t.dot(,)
t.goto(,)
t.circle()